dependsOn:
  - vue-recaptcha

tasks:
  prepare:
    command:
      - nuxt
      - prepare
    deps:
      - ^:build
  dev:
    command:
      - nuxt
      - dev
    deps:
      - ^:build
    local: true
