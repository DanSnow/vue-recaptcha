---
dependencies:
  pre:
    - npm install -g npm@3
deployment:
  release:
    branch: master
    commands:
      - npm run compile
      - git config user.email 'DanSnow@users.noreply.github.com'
      - git config user.name 'circleci'
      - "git add dist/vue-recaptcha.js && git commit -m 'ci: Compile [skip ci]' && git push || true"
